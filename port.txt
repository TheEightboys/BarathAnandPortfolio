<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Barath Anand | Tech Portfolio</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.css" />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/react/18.2.0/umd/react.production.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/react-dom/18.2.0/umd/react-dom.production.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/0.160.0/three.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/babel-standalone/7.23.1/babel.min.js"></script>
    <style>
        /* Font imports to get Spotify-like feel */
        @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700;800&display=swap');
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap');
        
        :root {
            /* New color scheme - dark blue and violet */
            --primary-color: #4721ed;
            --secondary-color: #9127f2;
            --accent-color: #25dedf;
            --dark-color: #0F0F2F;
            --darker-color: #080819;
            --light-color: #f0f2f5;
            --text-primary: #ffffff;
            --text-secondary: #e0e0e5;
            --text-muted: #a0a0c0;
            
            /* Transitions */
            --transition-normal: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
            --transition-bounce: all 0.5s cubic-bezier(0.68, -0.55, 0.265, 1.55);
            --transition-smooth: all 0.4s cubic-bezier(0.215, 0.61, 0.355, 1);
            
            /* Shadows */
            --shadow-sm: 0 4px 12px rgba(0, 0, 0, 0.1);
            --shadow-md: 0 8px 24px rgba(0, 0, 0, 0.15);
            --shadow-lg: 0 16px 36px rgba(0, 0, 0, 0.25);
            --shadow-glow: 0 0 20px rgba(71, 33, 237, 0.4);
            
            /* Gradients */
            --gradient-primary: linear-gradient(135deg, #4721ed, #9127f2);
            --gradient-secondary: linear-gradient(135deg, #25dedf, #4721ed);
            --gradient-dark: linear-gradient(135deg, #0F0F2F, #080819);
            --gradient-card: linear-gradient(145deg, rgba(24, 24, 48, 0.8), rgba(15, 15, 30, 0.95));
            
            /* Border radius */
            --border-radius-sm: 8px;
            --border-radius-md: 12px;
            --border-radius-lg: 20px;
            --border-radius-xl: 30px;
            --border-radius-pill: 50px;
        }

        /* Light mode overrides */
        [data-theme="light"] {
            --primary-color: #5025f5;
            --secondary-color: #9127f2;
            --dark-color: #f8f9ff;
            --darker-color: #edf0f9;
            --light-color: #ffffff;
            --text-primary: #111133;
            --text-secondary: #333355;
            --text-muted: #6c6c9c;
            --gradient-dark: linear-gradient(135deg, #f8f9ff, #edf0f9);
            --gradient-card: linear-gradient(145deg, rgba(255, 255, 255, 0.9), rgba(245, 245, 255, 0.95));
        }

        /* Base Styles */
        body {
            font-family: 'Montserrat', 'Segoe UI', sans-serif;
            background-color: var(--darker-color);
            color: var(--text-primary);
            margin: 0;
            padding: 0;
            overflow-x: hidden;
            scroll-behavior: smooth;
            transition: background-color 0.5s ease;
            line-height: 1.6;
            font-weight: 400;
        }

        h1, h2, h3, h4, h5, h6 {
            font-family: 'Poppins', 'Montserrat', sans-serif;
            font-weight: 700;
        }

        p {
            font-family: 'Montserrat', sans-serif;
            letter-spacing: 0.01em;
        }

        /* Background effects */
        #background-canvas {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 0;
            opacity: 0.3;
            transition: opacity 0.5s ease;
        }

        /* Animation container */
        .animation-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100vh;
            pointer-events: none;
            z-index: 1;
            overflow: hidden;
        }

        /* Tech Elements */
        .tech-element {
            position: absolute;
            opacity: 0.1;
            filter: blur(1px);
            pointer-events: none;
            transition: opacity 0.5s ease;
        }

        /* 3D objects container */
        #three-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 0;
            opacity: 0.6;
            pointer-events: none;
        }

        /* All containers need higher z-index */
        .container {
            position: relative;
            z-index: 2;
        }

        /* Preloader */
        .preloader {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--darker-color);
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 9999;
            transition: opacity 0.8s ease-out, visibility 0.8s ease-out;
        }

        .preloader.hidden {
            opacity: 0;
            visibility: hidden;
        }

        .loader {
            width: 80px;
            height: 80px;
            border: 4px solid rgba(71, 33, 237, 0.2);
            border-radius: 50%;
            border-top-color: var(--primary-color);
            box-shadow: var(--shadow-glow);
            animation: spin 1.2s infinite cubic-bezier(0.58, 0.23, 0.33, 0.86);
        }

        @keyframes spin {
            to {
                transform: rotate(360deg);
            }
        }

        /* Custom Cursor */
        .custom-cursor {
            position: fixed;
            width: 20px;
            height: 20px;
            border-radius: 50%;
            background: rgba(145, 39, 242, 0.25);
            border: 1px solid rgba(71, 33, 237, 0.5);
            backdrop-filter: blur(2px);
            pointer-events: none;
            z-index: 9999;
            transform: translate(-50%, -50%);
            transition: width 0.2s, height 0.2s, background 0.2s;
            display: none;
            mix-blend-mode: exclusion;
        }

        .custom-cursor::after {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 4px;
            height: 4px;
            background: rgba(255, 255, 255, 0.8);
            border-radius: 50%;
        }

        @media (hover: hover) {
            .custom-cursor {
                display: block;
            }
        }

        .custom-cursor.expand {
            width: 40px;
            height: 40px;
            background: rgba(145, 39, 242, 0.15);
            mix-blend-mode: normal;
        }

        .custom-cursor.link-hover {
            width: 30px;
            height: 30px;
            background: rgba(37, 222, 223, 0.25);
            border-color: var(--accent-color);
        }

        /* Theme toggle button */
        .theme-switch {
            position: fixed;
            bottom: 30px;
            left: 30px;
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background: var(--gradient-primary);
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            z-index: 100;
            box-shadow: var(--shadow-md);
            transition: var(--transition-bounce);
        }

        .theme-switch:hover {
            transform: translateY(-5px);
            box-shadow: var(--shadow-lg);
        }

        /* Navbar styling */
        .navbar {
            background-color: rgba(15, 15, 47, 0.85);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            box-shadow: var(--shadow-md);
            transition: var(--transition-smooth);
            padding: 15px 0;
            z-index: 1000;
        }

        [data-theme="light"] .navbar {
            background-color: rgba(248, 249, 255, 0.85);
        }

        .navbar.scrolled {
            padding: 10px 0;
            background-color: rgba(15, 15, 47, 0.95);
        }

        [data-theme="light"] .navbar.scrolled {
            background-color: rgba(248, 249, 255, 0.95);
        }

        .navbar-brand {
            font-size: 1.75rem;
            font-weight: 800;
            background: var(--gradient-primary);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            transition: var(--transition-normal);
            letter-spacing: -0.02em;
        }

        .navbar-brand:hover {
            transform: translateY(-2px);
            text-shadow: var(--shadow-glow);
        }

        .navbar-nav .nav-link {
            color: var(--text-primary);
            padding: 0.75rem 1.25rem;
            position: relative;
            transition: var(--transition-normal);
            font-weight: 500;
        }

        .navbar-nav .nav-link::after {
            content: '';
            position: absolute;
            width: 0;
            height: 2px;
            bottom: 10px;
            left: 50%;
            background: var(--gradient-primary);
            transition: width 0.3s ease, left 0.3s ease;
            border-radius: 2px;
        }

        .navbar-nav .nav-link:hover::after,
        .navbar-nav .nav-link.active::after {
            width: 60%;
            left: 20%;
        }

        .navbar-nav .nav-link:hover,
        .navbar-nav .nav-link.active {
            color: var(--primary-color);
        }

        /* Mobile nav */
        .navbar-toggler {
            border: none;
            padding: 0;
            outline: none !important;
            box-shadow: none !important;
        }

        .navbar-toggler-icon {
            background-image: none !important;
            position: relative;
            width: 24px;
            height: 18px;
        }

        .navbar-toggler-icon span {
            position: absolute;
            width: 100%;
            height: 2px;
            background-color: var(--text-primary);
            left: 0;
            transition: var(--transition-normal);
        }

        .navbar-toggler-icon span:nth-child(1) { top: 0; }
        .navbar-toggler-icon span:nth-child(2) { top: 8px; }
        .navbar-toggler-icon span:nth-child(3) { top: 16px; }

        .navbar-toggler[aria-expanded="true"] .navbar-toggler-icon span:nth-child(1) {
            transform: rotate(45deg) translate(5px, 5px);
        }

        .navbar-toggler[aria-expanded="true"] .navbar-toggler-icon span:nth-child(2) {
            opacity: 0;
        }

        .navbar-toggler[aria-expanded="true"] .navbar-toggler-icon span:nth-child(3) {
            transform: rotate(-45deg) translate(7px, -7px);
        }

        /* Hero section */
        #hero {
            background: var(--gradient-dark);
            background-size: 200% 200%;
            color: var(--text-primary);
            padding: 160px 0 140px;
            position: relative;
            overflow: hidden;
            animation: gradientAnimation 15s ease infinite;
        }

        @keyframes gradientAnimation {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        #hero::before {
            content: '';
            position: absolute;
            top: 0;
            right: 0;
            bottom: 0;
            left: 0;
            background-size: 100px 100px;
            opacity: 0.05;
            z-index: 0;
            transform: rotate(5deg) scale(1.2);
            background-image: radial-gradient(circle, var(--primary-color) 1px, transparent 1px);
        }

        #hero .container {
            position: relative;
            z-index: 1;
        }

        .profile-img-container {
            position: relative;
            width: 280px;
            height: 280px;
            margin: 0 auto;
        }

        .profile-img {
            width: 250px;
            height: 250px;
            border-radius: 50%;
            object-fit: cover;
            border: 4px solid rgba(255, 255, 255, 0.8);
            box-shadow: var(--shadow-lg), 0 0 30px rgba(71, 33, 237, 0.4);
            transition: transform 0.7s cubic-bezier(0.175, 0.885, 0.32, 1.275), box-shadow 0.7s ease;
            position: relative;
            z-index: 2;
            filter: brightness(1.05) contrast(1.05);
        }

        .profile-img:hover {
            transform: scale(1.05) translateY(-10px);
            box-shadow: var(--shadow-lg), 0 0 40px rgba(71, 33, 237, 0.6);
        }

        .profile-img-container::before {
            content: '';
            position: absolute;
            width: 300px;
            height: 300px;
            border-radius: 50%;
            background: conic-gradient(var(--primary-color), var(--accent-color), var(--secondary-color), var(--primary-color));
            top: -25px;
            left: -25px;
            z-index: 1;
            animation: rotate 8s linear infinite;
            opacity: 0.8;
        }

        @keyframes rotate {
            0% {
                transform: rotate(0deg);
            }
            100% {
                transform: rotate(360deg);
            }
        }

        .hero-title {
            font-size: 3.5rem;
            font-weight: 800;
            margin-bottom: 1rem;
            background: var(--gradient-primary);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            letter-spacing: -0.02em;
            text-shadow: var(--shadow-glow);
        }

        .typing-effect::after {
            content: '|';
            animation: blink 1s infinite;
            margin-left: 5px;
            color: var(--accent-color);
        }

        @keyframes blink {
            0%, 100% { opacity: 1; }
            50% { opacity: 0; }
        }

        .hero-subtitle {
            color: var(--text-secondary);
            font-size: 1.15rem;
            margin-bottom: 0.5rem;
            letter-spacing: 0.05em;
            font-weight: 400;
        }

        .hero-lead {
            color: var(--accent-color);
            font-size: 1.4rem;
            font-weight: 600;
            margin-bottom: 2rem;
            letter-spacing: 0.02em;
        }

        /* About section */
        #about {
            background-color: var(--dark-color);
            backdrop-filter: blur(10px);
            border-radius: var(--border-radius-lg);
            box-shadow: var(--shadow-lg);
            margin-top: -80px;
            position: relative;
            z-index: 10;
            padding: 60px 40px;
            transition: var(--transition-normal);
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        #about:hover {
            box-shadow: var(--shadow-lg), 0 5px 30px rgba(71, 33, 237, 0.15);
        }

        #about h2 {
            font-size: 2.5rem;
            color: var(--text-primary);
            position: relative;
            padding-bottom: 15px;
            margin-bottom: 25px;
            letter-spacing: -0.02em;
        }

        #about h2::after {
            content: '';
            position: absolute;
            width: 80px;
            height: 3px;
            background: var(--gradient-primary);
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            border-radius: 3px;
        }

        #about p.lead {
            font-size: 1.15rem;
            color: var(--text-secondary);
            max-width: 80%;
            margin: 0 auto 30px;
            line-height: 1.8;
        }

        #about .about-item {
            transition: var(--transition-smooth);
            padding: 30px 20px;
            border-radius: var(--border-radius-md);
            background: var(--gradient-card);
            border: 1px solid rgba(255, 255, 255, 0.05);
            height: 100%;
        }

        #about .about-item:hover {
            transform: translateY(-15px);
            box-shadow: var(--shadow-md);
        }

        #about .about-item i {
            transition: var(--transition-bounce);
        }

        #about .about-item:hover i {
            transform: scale(1.2);
        }

        #about .about-item h4 {
            font-size: 1.5rem;
            color: var(--text-primary);
            margin: 1rem 0;
            font-weight: 600;
        }

        #about .about-item p {
            color: var(--text-secondary);
            font-size: 1rem;
            line-height: 1.7;
        }

        /* Skills section */
        #skills {
            background: var(--gradient-dark);
            padding: 120px 0;
            position: relative;
        }

        #skills::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 100px;
            background: linear-gradient(to bottom, var(--dark-color) 0%, transparent 100%);
            z-index: 1;
        }

        #skills h2 {
            font-size: 2.5rem;
            color: var(--text-primary);
            margin-bottom: 60px;
            position: relative;
            padding-bottom: 15px;
            letter-spacing: -0.02em;
        }

        #skills h2::after {
            content: '';
            position: absolute;
            width: 80px;
            height: 3px;
            background: var(--gradient-primary);
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            border-radius: 3px;
        }

        .skill-item {
            background: var(--gradient-card);
            border-radius: var(--border-radius-md);
            box-shadow: var(--shadow-md);
            transition: var(--transition-smooth);
            position: relative;
            overflow: hidden;
            z-index: 1;
            border: 1px solid rgba(255, 255, 255, 0.05);
            padding: 30px 20px;
            height: 100%;
        }

        .skill-item::before {
            content: '';
            position: absolute;
            top: 100%;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--gradient-primary);
            opacity: 0.1;
            transition: var(--transition-normal);
            z-index: -1;
        }

        .skill-item:hover {
            transform: translateY(-15px) scale(1.02);
            box-shadow: var(--shadow-lg);
        }

        .skill-item:hover::before {
            top: 0;
        }

        .skill-item:hover .skill-icon {
            transform: rotateY(360deg);
            box-shadow: 0 10px 25px rgba(71, 33, 237, 0.4);
        }

        .skill-item:hover h4 {
            color: var(--accent-color);
        }

        .skill-icon {
            width: 80px;
            height: 80px;
            background: var(--gradient-primary);
            border-radius: var(--border-radius-md);
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 25px;
            color: white;
            font-size: 2rem;
            box-shadow: 0 10px 20px rgba(71, 33, 237, 0.3);
            transition: transform 0.8s cubic-bezier(0.175, 0.885, 0.32, 1.275), box-shadow 0.5s ease;
        }

        .skill-item h4 {
            font-size: 1.5rem;
            margin-bottom: 15px;
            transition: var(--transition-normal);
            color: var(--text-primary);
        }

        .skill-item p {
            color: var(--text-secondary);
            line-height: 1.7;
        }

        /* Projects section */
        #projects {
            background-color: var(--darker-color);
            padding: 120px 0;
            position: relative;
        }

        #projects::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 100px;
            background: linear-gradient(to bottom, var(--dark-color) 0%, transparent 100%);
            z-index: 1;
        }

        #projects .container {
            position: relative;
            z-index: 2;
        }

        #projects h2 {
            font-size: 2.5rem;
            color: var(--text-primary);
            margin-bottom: 60px;
            position: relative;
            padding-bottom: 15px;
            letter-spacing: -0.02em;
        }

        #projects h2::after {
            content: '';
            position: absolute;
            width: 80px;
            height: 3px;
            background: var(--gradient-primary);
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            border-radius: 3px;
        }

        .project-card {
            background: var(--gradient-card);
            border: none;
            border-radius: var(--border-radius-md);
            overflow: hidden;
            box-shadow: var(--shadow-md);
            transition: var(--transition-smooth);
            height: 100%;
            position: relative;
            top: 0;
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .project-card:hover {
            transform: translateY(-15px);
            box-shadow: var(--shadow-lg), 0 10px 30px rgba(71, 33, 237, 0.2);
        }

        .project-card .card-img-top {
            height: 200px;
            object-fit: cover;
            transition: var(--transition-normal);
            filter: contrast(1.05) brightness(1.05);
        }

        .project-card:hover .card-img-top {
            transform: scale(1.05);
        }

        .project-card .card-title {
            font-size: 1.5rem;
            color: var(--text-primary);
            font-weight: 600;
            margin-bottom: 15px;
            letter-spacing: -0.01em;
        }

        .project-card .card-body {
            padding: 25px;
        }

        .project-card .card-text {
            color: var(--text-secondary);
            margin-bottom: 20px;
            line-height: 1.7;
        }

        .project-card .badge {
            padding: 8px 15px;
            margin-right: 8px;
            margin-bottom: 8px;
            font-weight: 500;
            font-size: 0.8rem;
            border-radius: var(--border-radius-pill);
            background: var(--gradient-primary);
            box-shadow: var(--shadow-sm);
            transition: var(--transition-normal);
        }

        .project-card .badge:hover {
            transform: translateY(-3px);
            box-shadow: var(--shadow-md);
        }

        .loading-repo {
            padding: 40px;
            text-align: center;
            background: var(--gradient-card);
            border-radius: var(--border-radius-md);
            box-shadow: var(--shadow-md);
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        /* Connect section */
        #connect {
            background: var(--gradient-dark);
            padding: 120px 0;
            position: relative;
        }

        #connect::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 100px;
            background: linear-gradient(to bottom, var(--darker-color) 0%, transparent 100%);
            z-index: 1;
        }

        #connect h2 {
            font-size: 2.5rem;
            color: var(--text-primary);
            margin-bottom: 60px;
            position: relative;
            padding-bottom: 15px;
            letter-spacing: -0.02em;
        }

        #connect h2::after {
            content: '';
            position: absolute;
            width: 80px;
            height: 3px;
            background: var(--gradient-primary);
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            border-radius: 3px;
        }

        .social-card {
            background: var(--gradient-card);
            border: none;
            border-radius: var(--border-radius-md);
            box-shadow: var(--shadow-md);
            transition: var(--transition-smooth);
            position: relative;
            overflow: hidden;
            z-index: 1;
            height: 100%;
            border: 1px solid rgba(255, 255, 255, 0.05);
            padding: 30px 20px;
        }

        .social-card::before {
            content: '';
            position: absolute;
            top: 100%;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--gradient-primary);
            opacity: 0.1;
            transition: var(--transition-normal);
            z-index: -1;
        }

        .social-card:hover {
            transform: translateY(-15px) scale(1.02);
            box-shadow: var(--shadow-lg), 0 10px 30px rgba(71, 33, 237, 0.2);
        }

        .social-card:hover::before {
            top: 0;
        }

        .social-card:hover i {
            transform: scale(1.2);
        }

        .social-card i {
            transition: var(--transition-bounce);
            background: var(--gradient-primary);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            font-size: 2.5rem;
            margin-bottom: 20px;
        }

        .social-card h4 {
            color: var(--text-primary);
            font-size: 1.3rem;
            margin-bottom: 15px;
            font-weight: 600;
        }

        .social-card p {
            color: var(--text-secondary);
            margin-bottom: 20px;
            line-height: 1.7;
        }

        .location-badge {
    padding: 12px 25px;
    background: var(--gradient-primary);
    border-radius: var(--border-radius-pill);
    display: inline-block;
    color: white;
    font-weight: 500;
    margin-top: 20px;
    transition: var(--transition-bounce);
    box-shadow: var(--shadow-sm);
}

.location-badge:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-md);
}

.btn-connect {
    padding: 12px 30px;
    background: var(--gradient-primary);
    border: none;
    border-radius: var(--border-radius-pill);
    color: white;
    font-weight: 600;
    transition: var(--transition-bounce);
    text-decoration: none;
    display: inline-block;
    margin-top: 15px;
    box-shadow: var(--shadow-md);
}

.btn-connect:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-lg), 0 10px 25px rgba(71, 33, 237, 0.3);
    color: white;
}

/* Contact Form */
.contact-form {
    background: var(--gradient-card);
    border-radius: var(--border-radius-lg);
    box-shadow: var(--shadow-lg);
    padding: 40px;
    border: 1px solid rgba(255, 255, 255, 0.05);
    transition: var(--transition-normal);
}

.contact-form:hover {
    box-shadow: var(--shadow-lg), 0 15px 35px rgba(71, 33, 237, 0.2);
}

.contact-form .form-control {
    background-color: rgba(15, 15, 47, 0.6);
    border: 1px solid rgba(255, 255, 255, 0.1);
    color: var(--text-primary);
    border-radius: var(--border-radius-md);
    padding: 15px;
    transition: var(--transition-normal);
    margin-bottom: 20px;
}

[data-theme="light"] .contact-form .form-control {
    background-color: rgba(255, 255, 255, 0.8);
    border: 1px solid rgba(0, 0, 0, 0.1);
}

.contact-form .form-control:focus {
    background-color: rgba(15, 15, 47, 0.8);
    border-color: var(--primary-color);
    box-shadow: 0 0 15px rgba(71, 33, 237, 0.2);
}

[data-theme="light"] .contact-form .form-control:focus {
    background-color: rgba(255, 255, 255, 0.95);
}

.contact-form textarea.form-control {
    min-height: 150px;
    resize: vertical;
}

.contact-form label {
    color: var(--text-secondary);
    margin-bottom: 8px;
    font-weight: 500;
}

.contact-form .btn-submit {
    padding: 14px 35px;
    background: var(--gradient-primary);
    border: none;
    border-radius: var(--border-radius-pill);
    color: white;
    font-weight: 600;
    transition: var(--transition-bounce);
    width: 100%;
    box-shadow: var(--shadow-md);
}

.contact-form .btn-submit:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-lg), 0 10px 25px rgba(71, 33, 237, 0.3);
}

/* Footer */
footer {
    background: var(--dark-color);
    padding: 80px 0 30px;
    position: relative;
}

footer::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 100px;
    background: linear-gradient(to bottom, var(--darker-color) 0%, transparent 100%);
    z-index: 1;
}

footer .container {
    position: relative;
    z-index: 2;
}

.footer-links h5 {
    color: var(--text-primary);
    font-size: 1.3rem;
    margin-bottom: 25px;
    font-weight: 600;
    position: relative;
    padding-bottom: 15px;
}

.footer-links h5::after {
    content: '';
    position: absolute;
    width: 40px;
    height: 2px;
    background: var(--gradient-primary);
    bottom: 0;
    left: 0;
    border-radius: 2px;
}

.footer-links ul {
    list-style: none;
    padding: 0;
    margin: 0;
}

.footer-links ul li {
    margin-bottom: 15px;
}

.footer-links ul li a {
    color: var(--text-secondary);
    text-decoration: none;
    transition: var(--transition-normal);
    position: relative;
    padding-left: 18px;
}

.footer-links ul li a:hover {
    color: var(--primary-color);
    transform: translateX(5px);
}

.footer-links ul li a::before {
    content: '→';
    position: absolute;
    left: 0;
    color: var(--accent-color);
    opacity: 0;
    transition: var(--transition-normal);
}

.footer-links ul li a:hover::before {
    opacity: 1;
    transform: translateX(-5px);
}

.footer-social a {
    display: inline-block;
    width: 40px;
    height: 40px;
    background: var(--gradient-card);
    border-radius: 50%;
    margin-right: 10px;
    margin-bottom: 10px;
    text-align: center;
    line-height: 40px;
    color: var(--text-primary);
    transition: var(--transition-bounce);
    border: 1px solid rgba(255, 255, 255, 0.05);
    box-shadow: var(--shadow-sm);
}

.footer-social a:hover {
    transform: translateY(-5px);
    background: var(--gradient-primary);
    color: white;
    box-shadow: var(--shadow-md);
}

.copyright {
    color: var(--text-muted);
    font-size: 0.9rem;
    padding-top: 30px;
    margin-top: 50px;
    border-top: 1px solid rgba(255, 255, 255, 0.05);
}

/* Back to top button */
.back-to-top {
    position: fixed;
    bottom: 30px;
    right: 30px;
    width: 50px;
    height: 50px;
    background: var(--gradient-primary);
    border-radius: 50%;
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    z-index: 100;
    box-shadow: var(--shadow-md);
    transition: var(--transition-bounce);
    opacity: 0;
    visibility: hidden;
}

.back-to-top.show {
    opacity: 1;
    visibility: visible;
}

.back-to-top:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-lg);
}

/* Portfolio filter */
.portfolio-filter {
    margin-bottom: 40px;
}

.portfolio-filter .btn {
    padding: 10px 25px;
    border-radius: var(--border-radius-pill);
    margin: 5px;
    color: var(--text-secondary);
    background: var(--gradient-card);
    border: 1px solid rgba(255, 255, 255, 0.05);
    transition: var(--transition-normal);
    font-weight: 500;
    box-shadow: var(--shadow-sm);
}

.portfolio-filter .btn.active,
.portfolio-filter .btn:hover {
    background: var(--gradient-primary);
    color: white;
    box-shadow: var(--shadow-md);
}

/* Audio player */
.audio-player {
    background: var(--gradient-card);
    border-radius: var(--border-radius-md);
    padding: 20px;
    box-shadow: var(--shadow-md);
    transition: var(--transition-normal);
    border: 1px solid rgba(255, 255, 255, 0.05);
}

.audio-player:hover {
    box-shadow: var(--shadow-lg);
}

.audio-controls {
    display: flex;
    align-items: center;
    margin-top: 15px;
}

.play-btn {
    width: 50px;
    height: 50px;
    background: var(--gradient-primary);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    cursor: pointer;
    margin-right: 15px;
    box-shadow: var(--shadow-sm);
    transition: var(--transition-bounce);
}

.play-btn:hover {
    transform: scale(1.1);
    box-shadow: var(--shadow-md);
}

.audio-progress {
    flex-grow: 1;
    height: 6px;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 3px;
    position: relative;
    cursor: pointer;
}

.audio-progress-filled {
    height: 100%;
    background: var(--gradient-primary);
    border-radius: 3px;
    width: 0%;
    transition: width 0.1s linear;
}

.audio-time {
    margin-left: 15px;
    color: var(--text-muted);
    font-size: 0.9rem;
}

/* Responsive styles */
@media (max-width: 1199.98px) {
    .hero-title {
        font-size: 3rem;
    }
    
    .hero-lead {
        font-size: 1.3rem;
    }
    
    .profile-img-container {
        width: 240px;
        height: 240px;
    }
    
    .profile-img {
        width: 220px;
        height: 220px;
    }
    
    .profile-img-container::before {
        width: 260px;
        height: 260px;
        top: -20px;
        left: -20px;
    }
}

@media (max-width: 991.98px) {
    #hero {
        padding: 140px 0 100px;
    }
    
    .hero-title {
        font-size: 2.5rem;
    }
    
    .hero-subtitle {
        font-size: 1.1rem;
    }
    
    .hero-lead {
        font-size: 1.2rem;
    }
    
    #about, #skills, #projects, #connect {
        padding: 80px 0;
    }
    
    #about h2, #skills h2, #projects h2, #connect h2 {
        font-size: 2rem;
    }
    
    .skill-item, .project-card, .social-card {
        margin-bottom: 30px;
    }
}

@media (max-width: 767.98px) {
    #hero {
        padding: 100px 0 80px;
        text-align: center;
    }
    
    .hero-title {
        font-size: 2.2rem;
    }
    
    .hero-subtitle {
        font-size: 1rem;
    }
    
    .hero-lead {
        font-size: 1.1rem;
    }
    
    .profile-img-container {
        margin-bottom: 40px;
    }
    
    #about, #skills, #projects, #connect {
        padding: 60px 0;
    }
    
    #about {
        margin-top: -40px;
        padding: 40px 20px;
    }
    
    #about h2, #skills h2, #projects h2, #connect h2 {
        font-size: 1.8rem;
    }
    
    .footer-links {
        margin-bottom: 30px;
    }
}

@media (max-width: 575.98px) {
    .hero-title {
        font-size: 1.8rem;
    }
    
    .hero-subtitle {
        font-size: 0.9rem;
    }
    
    .hero-lead {
        font-size: 1rem;
    }
    
    .profile-img-container {
        width: 200px;
        height: 200px;
    }
    
    .profile-img {
        width: 180px;
        height: 180px;
    }
    
    .profile-img-container::before {
        width: 220px;
        height: 220px;
        top: -20px;
        left: -20px;
    }
    
    #about, #skills, #projects, #connect {
        padding: 50px 0;
    }
    
    #about {
        margin-top: -30px;
        padding: 30px 15px;
    }
    
    #about h2, #skills h2, #projects h2, #connect h2 {
        font-size: 1.6rem;
    }
    
    .contact-form {
        padding: 20px;
    }
    
    .portfolio-filter .btn {
        padding: 8px 15px;
        font-size: 0.9rem;
    }
}

/* Dark/Light mode transitions */
body.changing-theme {
    transition: background-color 0.5s ease, color 0.5s ease;
}

body.changing-theme * {
    transition: background-color 0.5s ease, color 0.5s ease, border-color 0.5s ease, box-shadow 0.5s ease;
}

/* Animations */
.animate__animated {
    animation-duration: 1s;
    animation-fill-mode: both;
}

@keyframes floatAnimation {
    0% {
        transform: translateY(0px);
    }
    50% {
        transform: translateY(-15px);
    }
    100% {
        transform: translateY(0px);
    }
}

.floating {
    animation: floatAnimation 4s ease-in-out infinite;
}

/* Loading animation */
.loading-spinner {
    display: inline-block;
    width: 50px;
    height: 50px;
    border: 3px solid rgba(71, 33, 237, 0.3);
    border-radius: 50%;
    border-top-color: var(--primary-color);
    animation: spin 1s ease-in-out infinite;
    margin-bottom: 20px;
}

/* Scrollbar styling */
::-webkit-scrollbar {
    width: 10px;
}

::-webkit-scrollbar-track {
    background: var(--darker-color);
}

::-webkit-scrollbar-thumb {
    background: var(--primary-color);
    border-radius: 5px;
}

::-webkit-scrollbar-thumb:hover {
    background: var(--secondary-color);
}

/* Toast notification */
.toast-notification {
    position: fixed;
    bottom: 30px;
    right: 30px;
    background: var(--gradient-card);
    border-left: 4px solid var(--accent-color);
    padding: 15px 25px;
    box-shadow: var(--shadow-lg);
    border-radius: var(--border-radius-md);
    z-index: 9999;
    opacity: 0;
    transform: translateY(20px);
    transition: opacity 0.5s ease, transform 0.5s ease;
}

.toast-notification.show {
    opacity: 1;
    transform: translateY(0);
}

.toast-notification.success {
    border-left-color: #25df7a;
}

.toast-notification.error {
    border-left-color: #ff5b5b;
}
    </style>
</head>

<body>
    <!-- Preloader -->
    <div class="preloader">
        <div class="loader"></div>
    </div>

    <!-- Custom cursor -->
    <div class="custom-cursor"></div>

    <!-- Background effects -->
    <div class="animation-container"></div>
    <canvas id="background-canvas"></canvas>
    <div id="three-container"></div>

    <!-- Back to top button -->
    <a href="#" class="back-to-top" aria-label="Back to top">
        <i class="fas fa-arrow-up"></i>
    </a>

    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg fixed-top navbar-dark">
        <div class="container">
            <a class="navbar-brand" href="#">
                <span>B</span><span>a</span><span>r</span><span>a</span><span>t</span><span>h</span>
                <span class="ms-2">A</span><span>n</span><span>a</span><span>n</span><span>d</span>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon">
                    <span></span>
                    <span></span>
                    <span></span>
                </span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link active" href="#hero">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#about">About</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#skills">Skills</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#projects">Projects</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#connect">Connect</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#contact">Contact</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
